<div id='overlay'>
    <div class="container_overlay" style="max-width: 70%; max-height: 70%">
      <img id='overlay-img' class="rounded" src="../{{meme.url}}">
      <p id="tags" style="float: left;">
        Tags: {% for tag in meme.tags %} {{tag}} {% endfor %}
      </p>
      <div style="float: right;">
        {% if current_user.id in meme.liked %}
        <a id='like_button{{meme.id}}' href="javascript:unlike({{meme.id}})" style="float:left;">
          <span id='heart{{meme.id}}' class="fas fa-heart" aria-hidden="true"></span>
        </a>
        {% else %}
        <a id='like_button{{meme.id}}' href="javascript:like({{meme.id}})" style="float:left;"><span id='heart{{meme.id}}'
            class="far fa-heart" aria-hidden="true"></span></a>
        {% endif %}
        <p id='like_count{{meme.id}}' style="float: left;">{{meme.likes}}</p>
      </div>
      <a href="../{{meme.url}}" download="meme{{meme.id}}" class="btn btn-primary">Download</a>
    </div>
  </div>

#overlay {
    position: fixed; /* Sit on top of the page content */
    display: none; /* Hidden by default */
    width: 100%; /* Full width (cover the whole page) */
    height: 100%; /* Full height (cover the whole page) */
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background-color: rgba(0,0,0,0.5); /* Black background with opacity */
    z-index: 2; /* Specify a stack order in case you're using a different order for other elements */
    cursor: pointer; /* Add a pointer on hover */
  }
  .container_overlay {
    position: absolute;
    top: 50%;
    left: 50%;
    -moz-transform: translateX(-50%) translateY(-50%);
    -webkit-transform: translateX(-50%) translateY(-50%);
    transform: translateX(-50%) translateY(-50%);