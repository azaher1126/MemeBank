Dev Docs:

To run the package you must install the dependencias using:
pip3 install -r requirments.txt
run main to start server and navigate to http://127.0.0.1:5000 in any browser

To reset all memes:
Delete database.db and delete all images from /static/memes